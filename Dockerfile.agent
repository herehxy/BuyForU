# Agent 运行镜像。先在仓库根目录执行 ./mvnw -pl agent-app -am package
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY agent-app/target/agent-app-0.1.0-SNAPSHOT.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
